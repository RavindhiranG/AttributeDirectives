import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})



export class AppComponent {
  title = 'AttributeDirectives';

  color = '';

  // The color property in the AppComponent class is used to store the currently selected color from the radio buttons in the template
}
